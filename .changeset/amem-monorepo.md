---
"openclaw-amem": minor
---

Repackage as the `amem` pnpm monorepo and extract the memory engine into `@heichaowo/amem-core` (bundled into the plugin, so there is no install or runtime change for users). New baseline `1.1.0` following the ClawHub 1.0.x line.
