---
---

No release needed. This adds `@heichaowo/amem-api`, a new private package that is
never published and that nothing in `openclaw-amem` imports yet. The plugin and
its bundled engine are unchanged.
